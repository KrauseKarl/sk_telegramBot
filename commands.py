from aiogram.types import BotCommand

private = [
    BotCommand(command='menu', description='Вот меню'),
    BotCommand(command='search', description='поиск'),
    BotCommand(command='category', description='категории'),
]
